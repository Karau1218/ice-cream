<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmed</title>
    <link rel="stylesheet" href="styles/styles.css">
    <link rel="icon" href="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSoKHnXPENvCGbSh39wRXLH3ZS24CSY1w32TA&s">
</head>
<body class = "conf-page">
    <div class="confirmation-container">
        <div class="checkmark-circle">
            <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                <circle class="checkmark-circle-path" cx="26" cy="26" r="25" fill="none"/>
                <path class="checkmark-check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
            </svg>
        </div>
        
        <h1 class="thank-you-header">Thank You for Your Order!</h1>
        <p class="confirmation-subtext">Your ice cream order has been confirmed</p>
        
        <div class="order-summary-box">
            
            <h3 class="section-title">PERSONAL INFORMATION</h3>
            <div class="detail-row">
                <span class="detail-label">Name:</span>
                <span class="detail-value"><%= order.fname %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Email:</span>
                <span class="detail-value"><%= order.email %></span>
            </div>

            <hr>
            
            <h3 class="section-title">ORDER DETAILS</h3>
            <div class="detail-row">
                <span class="detail-label">Flavor:</span>
                <span class="detail-value"><%= order.Flavor.replace('flavor', '') %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Cone Type:</span>
                <span class="detail-value"><%= order.method.charAt(0).toUpperCase() + order.method.slice(1) %> Cone</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Toppings:</span>
                <div class="toppings-list">
                    <% if (Array.isArray(order.toppings)) { %>
                        <% order.toppings.forEach(topping => { %>
                            <span class="topping-tag"><%= topping.charAt(0).toUpperCase() + topping.slice(1) %></span>
                        <% }); %>
                    <% } else if (order.toppings) { %>
                        <span class="topping-tag"><%= order.toppings.charAt(0).toUpperCase() + order.toppings.slice(1) %></span>
                    <% } %>
                </div>
            </div>

            <hr>

            <h3 class="section-title">ADDITIONAL COMMENTS</h3>
            <div class="comments-box">
                <%= order.comment %>
            </div>
        </div>

        <div class="button-container">
            <button class="print-button" onclick="window.print()">Print Order</button>
            <a href="/home" class="place-another-button" role="button">Place Another Order</a>
        </div>
        
    </div>
</body>
</html>